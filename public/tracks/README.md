# Track cover images

Drop each program's image here, named by the track id:

- `salary-negotiation.png`  ← the Malay rupee/coins image
- `managing-up.png`
- `toxic-workplace.png`
- `first-90-days.png`

Square (1:1) works best — the UI crops to fit and biases toward the top so the face stays visible.
Reference them in `src/lib/mock-data.ts` via `image: '/tracks/<id>.png'`.
